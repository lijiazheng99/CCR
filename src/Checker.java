public class Checker {
    /*
    private Color checkerColor;
    private int position;
    private int pointsNeeded;

    public Checker(Color cheCol, int pos)
    {
        this.checkerColor = cheCol;
        this.position = pos;
        if(this.checkerColor == Color.Red)
            this.pointsNeeded = 24 - pos;
        else if(this.checkerColor == Color.White)
            this.pointsNeeded = pos;
        else
            System.out.println("Error in Checker Setting");
    }

    public int moveTo(int points)
    {
        if(this.getColor() == Color.Red)
            return this.position + points;
        else if(this.getColor() == Color.White)
            return this.position - points;
        else
            System.out.println("Error in Checker Moving");
    }

    public Color getColor()
    {
        return this.checkerColor;
    }
    */

    //Don't think we will need Checkers for functional moving.


}
